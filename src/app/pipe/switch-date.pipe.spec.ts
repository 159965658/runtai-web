import { SwitchDatePipe } from './switch-date.pipe';

describe('SwitchDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchDatePipe();
    expect(pipe).toBeTruthy();
  });
});
