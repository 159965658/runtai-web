import { MySaleFromPipe } from './my-sale-from.pipe';

describe('MySaleFromPipe', () => {
  it('create an instance', () => {
    const pipe = new MySaleFromPipe();
    expect(pipe).toBeTruthy();
  });
});
