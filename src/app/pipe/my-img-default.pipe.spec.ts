import { MyImgDefaultPipe } from './my-img-default.pipe';

describe('MyImgDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new MyImgDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
